{% set variant = params.variant if params.variant %}

<i 
    class="bc-icon fa-solid fa-{{ variant }} {%- if params.classes %} {{ params.classes }}{% endif %}"
    {%- for attribute, value in params.attributes %} {{ attribute }}="{{ value }}"{% endfor %}>
>
</i>