params:
  - name: attributes
    type: object
    description: Properties and values to add to the component as HTML attributes, expressed as key/value pairs.
  - name: classes
    type: string
    description: Classes to add to the component.
  - name: describedBy
    type: string
    description: A string of additional element IDs to add to the `aria-describedby` attribute.
  - name: idPrefix
    type: string
    default: "Date"
    description: A string used to prefix IDs generated by this component. This can be useful if multiple Date Inputs have to appear on a single page.
  - name: formRow
    type: object
    default: Dependent on value of `error` parameter.
    description: Configuration object for the wrapping Form Row component. Parameters are passed to the Form Row component. *Note that the `error` parameter is automatically set based on the `error` parameter, and cannot be overridden.*
    seeAlso: [form-row]
  - name: fieldset
    type: object
    required: true
    description: Configuration object for the wrapping Fieldset component.
    seeAlso: [fieldset]
  - name: hint
    type: object
    description: Hint copy, providing further information on what information is being requested and what format it may need to be provided in. Parameters are passed to the Form Hint component. *Note that the `id` parameter is set automatically, and cannot be overridden.*
    seeAlso: [form-hint]
  - name: error
    type: object
    description: Error copy, describing why the provided input isn't adequate for our needs and applies error styling to the inputs and Form Row. Parameters are passed to the Form Error component. *Note that the `id` parameter is set automatically, and cannot be overridden.*
    seeAlso: [form-error]
  - name: day
    type: object
    description: Configuration for the day input. If left empty, the day input will not be rendered.
    params:
      - name: name
        type: string
        required: true
        description: The name of the day input. This is used as the identifying key when sending the form to the server and should be unique to the document.
      - name: value
        type: string
        description: A pre-populated value for the input. A user can overwrite this value.
      - name: autocomplete
        type: string
        description: This attribute provides a hint to assistive technologies as to what type of information is being requested, in case it can fill it automatically. This must be [one of a specific set of values](https://developer.mozilla.org/en-US/docs/Web/HTML/Attributes/autocomplete) to work.
      - name: attributes
        type: object
        description: Properties and values to add to the input as HTML attributes, expressed as key/value pairs.
  - name: month
    type: object
    description: Configuration for the month input. If left empty, the month input will not be rendered.
    params:
      - name: name
        type: string
        required: true
        description: The name of the month input. This is used as the identifying key when sending the form to the server and should be unique to the document.
      - name: value
        type: string
        description: A pre-populated value for the input. A user can overwrite this value.
      - name: autocomplete
        type: string
        description: This attribute provides a hint to assistive technologies as to what type of information is being requested, in case it can fill it automatically. This must be [one of a specific set of values](https://developer.mozilla.org/en-US/docs/Web/HTML/Attributes/autocomplete) to work.
      - name: attributes
        type: object
        description: Properties and values to add to the input as HTML attributes, expressed as key/value pairs.
  - name: year
    type: object
    description: Configuration for the year input. If left empty, the year input will not be rendered.
    params:
      - name: name
        type: string
        required: true
        description: The name of the year input. This is used as the identifying key when sending the form to the server and should be unique to the document.
      - name: value
        type: string
        description: A pre-populated value for the input. A user can overwrite this value.
      - name: autocomplete
        type: string
        description: This attribute provides a hint to assistive technologies as to what type of information is being requested, in case it can fill it automatically. This must be [one of a specific set of values](https://developer.mozilla.org/en-US/docs/Web/HTML/Attributes/autocomplete) to work.
      - name: attributes
        type: object
        description: Properties and values to add to the input as HTML attributes, expressed as key/value pairs.
