<img
    data-module="uky-logo"
    class="uky-logo {%- if params.classes %} {{ params.classes }}{% endif %}"
    alt="{{ params.alt }}"
	src="{{ params.src }}"
    {%- for attribute, value in params.attributes %} {{ attribute }}="{{ value }}"{% endfor %}>
